services:
  - type: web
    name: garage-rmi-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand:gunicorn main:app 
    envVars:
    - key:python_VERSION
    value: 3.10.0
